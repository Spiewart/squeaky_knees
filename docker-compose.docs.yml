services:
  docs:
    image: squeaky_knees_local_docs
    container_name: squeaky_knees_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - /app/.venv
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./squeaky_knees:/app/squeaky_knees:z
    ports:
      - '9000:9000'
    command: /start-docs
